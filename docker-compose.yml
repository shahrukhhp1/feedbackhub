services:
  postgres:
    image: postgres:15-alpine
    container_name: feedbackhub-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: feedbackhub
    volumes:
      - feedbackhub_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d feedbackhub"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  feedbackhub_pgdata:
